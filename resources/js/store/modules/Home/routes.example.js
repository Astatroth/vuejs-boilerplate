// Importing middleware
// import {auth, subscribed, permission} from 'ROOT/Middleware';

const routes = [
    // Example module route
    /*{
        path: '/some-path',
        name: 'path-name',
        component: () => import(/!* webpackChunkName: 'component-chunk-name' *!/ './component'),
        meta: {
            breadcrumbs: [
                {
                    label: 'Home'
                }
            ],
            title: 'Title',
            middleware: [auth, permission],
            permissions: ['access-page'],
        }
    }*/
];

export default routes;